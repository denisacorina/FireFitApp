<div class="top-navbar">
    <img class="app-logo" src="/images/fire-flame-logo-design-template-vector.png" alt="FireFit Logo" />
    <span class="app-title">Fire<span class="fit-gradient">Fit</span></span>
</div>

<style>
    .top-navbar {
        display: flex;
        align-items: center;
        justify-content: center;
        height: 56px;
        background: #fff;
        box-shadow: 0 2px 12px rgba(255,87,34,0.07);

        position: sticky;
        top: 0;
        z-index: 1000;
        padding: 0 1rem;
    }

    .app-logo {
        width: 38px;
        height: 38px;
        border-radius: 10px;
        margin-right: 12px;
        background: #fff7f0;
        object-fit: contain;
    }

    .app-title {
        font-size: 1.5rem;
        font-weight: 800;
        letter-spacing: 1px;
        color: #ff5722;
        font-family: 'Inter', Arial, sans-serif;
    }

   /*  .fit-gradient {
        background: linear-gradient(90deg, #ff9800 0%, #ff5722 100%);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        background-clip: text;
    } */
    @@media (max-width: 480px) {
        .top-navbar

    {
        height: 48px;
        padding: 0 0.5rem;
    }

    .app-logo {
        width: 28px;
        height: 28px;
        margin-right: 8px;
    }

    .app-title {
        font-size: 1.1rem;
    }

    }</style>
